---
const {img, title, description, price} = Astro.props
---

<div class="border rounded justify-center p-3 text-center">
  <img class="h-52 rounded" src={img} alt="foto">
  <p class="fs-3 text-center" >{title}</p>
  <p class="text-secondary text-center">{description}</p>
  <p class="text-secondary text-center">$ {price}</p>
  <button class="border rounded p-4">Agregar</button>
</div>